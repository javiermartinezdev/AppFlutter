import 'package:flutter/material.dart';
import 'app.dart';

/// Punto de entrada principal de la aplicación.
///
/// La función `main` es la primera que se ejecuta cuando se inicia
/// una aplicación Flutter. Aquí se llama a `runApp`, que infla el widget
/// raíz de la aplicación (en este caso, `MyApp`).
void main() {
  runApp(const MyApp());
}
